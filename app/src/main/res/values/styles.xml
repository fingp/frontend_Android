<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/colorWhite </item>
        <item name="colorPrimary">@color/defualtColor</item>
        <item name="colorPrimaryDark">@color/defualtColor</item>
        <item name="colorAccent">@color/defualtColor</item>
        <item name="android:colorButtonNormal">@color/defualtColor</item>
    </style>

</resources>
